Problem 3. (20 points)
Use selection (i.e. average runtime of O(n)) to find the ith smallest number. The first number in the
text file (on its own line) will be УiФ. The second line will the the array of number which you want to
find the ith smallest. You may assume the array contains only integers.
Sample output.txt (single number only):
5
Sample input.txt (first line will be the integer УiФ, second line will be array with spaces between
elements (no space after last)):
7
4а5а8а2а4а7а5а0а8а2а3а9а23а48ан12а49